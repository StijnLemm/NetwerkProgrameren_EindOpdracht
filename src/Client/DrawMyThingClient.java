package Client;

import javafx.scene.canvas.GraphicsContext;

public class DrawMyThingClient {

    private GameContainer gameContainer;

    public DrawMyThingClient(GameContainer gameContainer) {
        this.gameContainer = gameContainer;
    }
}
